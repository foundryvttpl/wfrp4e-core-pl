if (args.actor.has("Bestial")) 
{
  this.script.notification(`${args.actor.prototypeToken.name} musi zdac <b>Trudny (-10) Test Sily Woli</b>, aby zaatakowac ten cel!`); 
}

return true; // No need to show this in the dialog
